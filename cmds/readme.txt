This directory is where you can write and include your
own gun commands. We use this to also include the
cellstack module, which is kept separately as it will
eventually be packaged as a remote module. 
